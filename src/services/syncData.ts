import supabase from "./externalDatabase";
import LocalDatabase, { Contact, SatisfactionSurveyPartner, SatisfactionSurveyNoPartner } from "./localDatabase";

export async function syncDataToRemote() {
  console.log("Teste")
}